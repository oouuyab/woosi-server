export enum USER_TYPE {
  DEFAULT = 0,
  HOST = 1,
  ADMIN = 2,
}

export enum USER_STATUS {
  ACTIVE = 1,
  INACTIVE = 2,
  LEAVE = 3,
}

export enum ROLE {
  ADMIN = 'admin',
  HOST = 'host',
  COMMON_USER = 'common_user',
}
